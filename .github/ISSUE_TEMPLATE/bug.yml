name: "Bug / correction"
description: "Report a mistake or suggest a correction"
labels: ["bug"]
body:
  - type: textarea
    id: what
    attributes:
      label: What is wrong?
    validations:
      required: true
  - type: textarea
    id: sources
    attributes:
      label: Sources
      description: "Provide references that support the correction"
